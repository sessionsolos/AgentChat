# Database — SQLite for local dev; swap path to postgresql://... for Postgres
DATABASE_URL="file:./dev.db"

# College Scorecard API key — get one at https://api.data.gov/signup
# Required for WS-1 Scorecard datasource; safe to omit in tests (stubs return [])
DATA_GOV_API_KEY="your-api-key-here"
