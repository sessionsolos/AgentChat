/**
 * Home page — placeholder for WS-3 (frontend).
 * WS-3 will replace this with the StudentProfile form and results UI.
 */

export default function HomePage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center p-8">
      <h1 className="text-3xl font-bold mb-4">Scholarship Finder</h1>
      <p className="text-gray-500 text-center max-w-md">
        Find scholarships and financial aid matched to your profile.
        The student profile form is coming in the next workstream.
      </p>
    </main>
  );
}
